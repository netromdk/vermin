name: "CodeQL"

# On all pushes/PRs to master branche and every sunday at 2:34.
on:
  push:
    branches: [ master ]
  pull_request:
    # The branches below must be a subset of the branches above
    branches: [ master ]
  schedule:
    - cron: '34 2 * * 6'

permissions:
  contents: read

jobs:
  analyze:
    name: Analyze
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read
      security-events: write

    strategy:
      fail-fast: false
      matrix:
        language: [ 'python' ]

    steps:
    - name: Checkout repository
      uses: actions/checkout@7884fcad6b5d53d10323aee724dc68d8b9096a2e

    - name: Initialize CodeQL
      uses: github/codeql-action/init@7cce9bbfb550e880420fd42bdb2f6a2908df139e
      with:
        languages: ${{ matrix.language }}

    - name: Autobuild
      uses: github/codeql-action/autobuild@7cce9bbfb550e880420fd42bdb2f6a2908df139e

    - name: Perform CodeQL Analysis
      uses: github/codeql-action/analyze@7cce9bbfb550e880420fd42bdb2f6a2908df139e
